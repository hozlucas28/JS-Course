let miTexto = 'Java-Script';

miTexto.replace('S', 's'); //Remplaza el primer argumento por el segundo.

miTexto.toUpperCase(); //Convierte en mayusculas el string.

miTexto.toLowerCase(); //Convierte en minusculas el string.

miTexto.split(''); //Convierte el string y separa las letras en un array.

miTexto = 'Hola Mundo de Java-Script';

miTexto.split(' '); //Convierte el string y separa las palabras (por el argumento) en un array.


myArray.reverse(); //Invierte el orden de los elementos del array, del ultimo elemento al primero.

myArray.join(); //Convierte el array en un string, por defecto con comas entre los elementos.

myArray.join(' '); //Convierte el array en un string, por el argumento, con espacios entre los elementos.
